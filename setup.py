from setuptools import setup


setup_requires = [
        ]

install_requires = [
        'ipython',
        ]

setup(
        name='jadeapi',
        version=0.1,
        install_requires=install_requires,
        )
